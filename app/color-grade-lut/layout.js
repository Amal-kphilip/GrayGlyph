export const metadata = {
  title: "Color Grade Transfer | GrayGlyph",
  description:
    "Transfer color grades between images with a 3D LUT workflow. Client-side processing with no uploads.",
  alternates: {
    canonical: "/color-grade-lut"
  }
};

export default function ColorGradeLayout({ children }) {
  return children;
}
