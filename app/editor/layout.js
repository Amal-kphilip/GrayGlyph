export const metadata = {
    title: "Free Online Photo Editor | Color Grading & Tone Curves | GrayGlyph",
    description: "Professional browser-based image editor. Lightroom-style color grading, tone curves, and presets. 100% private photo editor with no upload required.",
};

export default function EditorLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}
