export const metadata = {
  title: "Color Transfer | GrayGlyph",
  description:
    "Transfer color mood from one image to another with browser-based 3D LUT processing and instant preview.",
  alternates: {
    canonical: "/color-transfer"
  }
};

export default function ColorTransferLayout({ children }) {
  return children;
}
